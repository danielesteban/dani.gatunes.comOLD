language: node_js
node_js: "lts/*"
cache: yarn
script:
 - yarn lint
 - yarn build
deploy:
  on:
    branch: master
  provider: script
  script: gh-pages -d dist -u "Travis CI <travis@travis-ci.org>" -r https://${GH_TOKEN}@github.com/danielesteban/dani.gatunes.com.git
  skip_cleanup: true
